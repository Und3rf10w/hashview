!!!
%html
  %body
    .span15
      .container
        .col-md-12
          .row
            .page-header
              %h1
                Pcap Files
            .row
              .col-md-10.pull-left
                The following are a list of Pcap Files currently configured in Hashview. All Pcap Files are assigned to a single customer. Deleting a Pcap File will remove it from the customer, but all cracked hashes will remain in the database.
      %br
      %br
      .container
        .row
          .col-md-12
            .table
              %table{class: 'table table'}
                %thead
                  %tr
                    %th
                      %b Customer
                    %th
                      %b Pcap File
                    %th
                      %b Crack Rate
                    %th
                      %b Control
                %tbody
                  - if @customers
                    - @customers.each do |customer|
                      %tr
                        %td #{customer.name}
                        %td
                        %td
                      - @pcaps.each do |pcap|
                        - if pcap.customer_id.to_i == customer.id.to_i
                          %td
                          %td #{pcap.name}
                          %td 
                            #{@cracked_status[pcap.id]}
                          %td
                            %a.btn.btn-primary{href: "/analytics?customer_id=#{customer.id}&pcap_id=#{pcap.id}"}
                              %i.glyphicon.glyphicon-stats{title: 'Display crack stats'}
                            %a.btn.btn-danger{href: "/pcaps/delete?pcap_id=#{pcap.id}"}
                              %i.glyphicon.glyphicon-trash{title: 'Delete'}
                          %tr
                        %tr
